CREATE TABLE users
(
    user_id serial NOT NULL,
    user_name character varying NOT NULL,
    display_name character varying,
    password_hash character varying(12) NOT NULL,
    colour1 integer NOT NULL DEFAULT 0,
    colour2 integer NOT NULL DEFAULT 0,
    PRIMARY KEY (user_id)
);